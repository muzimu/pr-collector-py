# PRCollect 配置文件模板
# 复制为 config.yaml 并填入真实值

github:
  token: "your_github_token_here"  # GitHub Personal Access Token，提升 API 速率限制至 5000次/小时

user:
  username: "muzimu"              # 要统计 PR 的 GitHub 用户名

output:
  target: "test.md"                 # 输出的 markdown 文件路径
